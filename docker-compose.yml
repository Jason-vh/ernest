services:
  app:
    build: .
    restart: unless-stopped
    environment:
      PORT: 3000
    ports:
      - "127.0.0.1:${ERNEST_APP_HOST_PORT:-3004}:3000"
    networks:
      - apps-net

networks:
  apps-net:
    external: true
